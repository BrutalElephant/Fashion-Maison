NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
CURRENCYAPI_KEY=
PAYSTACK_SECRET_KEY=
FX_REFRESH_INTERVAL_MINUTES=60
FX_FALLBACK_WINDOW_HOURS=24
# Gate for scheduled update-fx-rate calls (send as x-fx-secret header).
FX_SCHEDULER_SECRET=
# Edge Functions read their secrets via `supabase secrets set` — these names are listed for ops reference only.
# SUPABASE_SERVICE_ROLE_KEY (Edge Functions only — never in this file in production, never in the browser)
